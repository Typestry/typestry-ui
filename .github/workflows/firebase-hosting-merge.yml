# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set Environment Variables
        run: |
          echo "VITE_APP_API_KEY=${{ secrets.VITE_APP_API_KEY }}" >> .env.production
          echo "VITE_APP_AUTH_DOMAIN=${{ secrets.VITE_APP_AUTH_DOMAIN }}" >> .env.production
          echo "VITE_APP_PROJECT_ID=${{ secrets.VITE_APP_PROJECT_ID }}" >> .env.production
          echo "VITE_APP_STORAGE_BUCKET=${{ secrets.VITE_APP_STORAGE_BUCKET }}" >> .env.production
          echo "VITE_APP_MESSAGING_SENDER_ID=${{ secrets.VITE_APP_MESSAGING_SENDER_ID }}" >> .env.production
          echo "VITE_APP_APP_ID=${{ secrets.VITE_APP_APP_ID }}" >> .env.production
          echo "VITE_APP_MEASUREMENT_ID=${{ secrets.VITE_APP_MEASUREMENT_ID }}" >> .env.production
          echo "VITE_APP_FUNCTIONS_URL=${{ secrets.VITE_APP_FUNCTIONS_URL }}" >> .env.production
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_CARRIEDBYBEES_923B7 }}"
          channelId: live
          projectId: carriedbybees-923b7
